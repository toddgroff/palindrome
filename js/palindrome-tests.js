// Write unit tests to test your isPalindrome solution.
//
// Test positive and negative cases
// What should happen if you pass null or undefined to isPalindrome? (Test that scenario)

describe('isPalindrome', function () {

  // Each individual test follows this pattern:
  it('returns true or false', function () {
    // A test should have at least one expectation.
    // Here, we say that we expect fizzbuzz().length
    // to be 100. If it isn't, this will error.
    expect(isPalindrome(str) == (true || false));
  });

  // it(''irst 15 numbers are correct', function () {
  // });
  //
  // it('returns the correct sequence', function () {
  // });
});
